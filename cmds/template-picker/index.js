var fs = require('fs'),
	path = require('path')
	;
var nf = require('node-file');


exports.execute = function(task, config){
	throw 'templatepicker not finish yet.'
	// var src, files;
	// for (var i = 0; i < task.source.length; i++) {
	// 	src = task.source[i].trim();
	// 	if(!src){
	// 		continue;
	// 	}
	// 	if(fs.statSync(src).isFile()){

	// 	}else{
			
	// 	}
	// }
	
}